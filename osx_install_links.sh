#!/bin/bash

ln -si $(pwd)/vim/vimrc ~/.vimrc
ln -si $(pwd)/tmux/tmux.conf ~/.tmux.conf
ln -si $(pwd)/bash/bashrc ~/.bashrc
ln -si $(pwd)/bash/bash_profile ~/.bash_profile
ln -si $(pwd)/bash/bash_aliases ~/.bash_aliases
